interface:
  display_name: "Task Spec Execution (Deprecated)"
  short_description: "Compatibility shim that redirects old task-spec-execution calls into split stages"
  default_prompt: "Use $task-spec-execution only as a compatibility shim. Redirect spec or plan work to $task-preparation, and redirect direct implementation work to $task-execution-simple."
