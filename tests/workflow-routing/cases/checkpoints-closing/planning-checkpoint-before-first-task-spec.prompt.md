# Planning Checkpoint Before First Task Spec

Use doc-driven-spec-workflow.

`M1` has already been reviewed and closed. `docs/tasks/m2-private-beta/index.md` already says `Roadmap confirmed: yes`, but `M2` did not yet have any concrete tasks. In this same conversation, the agent ran `milestone-planning` for `M2` and created `docs/tasks/m2-private-beta/authentication/01-login-session/task.md` as the first concrete task in that milestone. The agent then stopped and reported that the task-planning docs changes must be checkpointed before entering task-local spec work. The user first says `continue`. After whatever the agent does next, the user then says `continue` again. In the initial response, explain what the first `continue` should do and what the second `continue` should do.

If the correct next step involves drafting any document, do not actually draft the document body in the response; only state that drafting that document is the intended next action.
