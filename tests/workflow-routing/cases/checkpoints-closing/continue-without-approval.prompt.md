# Continue Without Approval

## Prompt

Use doc-driven-spec-workflow.

The agent has just drafted a task-local `spec.md`, but the user has not approved it yet.

The selected task has no plan trigger: it does not involve migration, compatibility sequencing, rollout coordination, or other non-obvious implementation ordering. No implementation has started yet.

The user now says:

continue
